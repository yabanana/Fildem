# Fedora Packaging (Rewrite)

Spec should package:

1. daemon binary `fildemd`
2. HUD binary `fildemhud`
3. interface XML files under `%{_datadir}/fildem/interfaces`
4. GNOME shell extension under `%{_datadir}/gnome-shell/extensions/fildem-shell@fildem.org`
5. desktop entry and user systemd unit
